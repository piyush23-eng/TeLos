services:
  - type: web
    name: telos
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:prod
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8787
      - key: AUTH_SESSION_SECRET
        generateValue: true
